---
title: LC 647 - Palindromic Substrings
date: 2024-09-27 0000:00:00 +0800
categories: [LeetCode, NeetCode150]
tags: [leetcode, neetcode150, 1d_dp]     # TAG names should always be lowercase
toc: true
math: true
mermaid: true
---

## Question

Given a string `s`, return _the number of **palindromic substrings** in it_.

A string is a **palindrome** when it reads the same backward as forward.

A **substring** is a contiguous sequence of characters within the string.

**Example 1:**

    Input: s = "abc"
    Output: 3

**Explanation:** Three palindromic strings: "a", "b", "c".

**Example 2:**

    Input: s = "aaa"
    Output: 6

**Explanation:** Six palindromic strings: "a", "a", "a", "aa", "aa", "aaa".

**Constraints:**

- `1 <= s.length <= 1000`
- `s` consists of lowercase English letters.

## Links
Question [here](https://leetcode.com/problems/palindromic-substrings/description/) and solution [here](https://github.com/etherion-1337/LeetCode/blob/master/medium/647_palindromic_substrings/solution.py)

## Solution

### concept
For the two pointer solution, it is quite similar to [[5. Longest Palindromic Substring]].

### code
```python
class Solution:
    def count_palin(self, s, l, r):
        result =  0
        while l >= 0 and r < len(s) and s[l] == s[r]:
            result += 1
            l -= 1
            r += 1
        return result

    def countSubstrings(self, s: str) -> int:
        result = 0

        for i in range(len(s)):
            l, r = i, i
            result += self.count_palin(s, l, r)

            l, r = i, i + 1
            result += self.count_palin(s,l, r)

        return result
```

## Complexity
time: $O(n^2)$      
space: $O(1)$ 